import React from 'react'
import axios from 'axios'

function handleCardFetch() {
 const baseURL = 
 process.env.NODE_ENV === "development"
 ? "http://localhost:3000/api"
 : "DEPLOYED LOCATION"
 


    return (
        <div>
            
        </div>
    )
}

export default handleCardFetch
